# Evaluation

Use synthetic labeled examples. Measure precision, recall, F1, false positives, false negatives, latency, anonymization correctness and response leakage detection. Compare Regex; Regex + spaCy; Regex + spaCy + Transformer. Do not report unmeasured performance numbers.
